import 'package:flutter/material.dart';

class challenge extends StatelessWidget {
  const challenge({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Text('Ola mundo 2'),
    );
  }
}
